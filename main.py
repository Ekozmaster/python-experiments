import mandelbrot
from matplotlib import pyplot as plt
from utils import DisplayResolutions
from PIL import Image

#img = mandelbrot.generate_mandelbrot_set(DisplayResolutions.h_1080, fractal_itterations=40, cam_pos=[-0.85, 0.2], cam_scale=2.5)
#img = img.resize(DisplayResolutions.h_1080, Image.ANTIALIAS)
#img.save('renders/mandelbrot_fhd2.jpg', subsampling=0, quality=100)
#plt.imshow(img)
#plt.show()